# Plugins

Plugin categories:
- `telephony/` for call intake systems
- `ticket/` for ticket destination systems
- `speech/` for optional transcription

Each plugin should implement a stable interface contract from `core/`.
